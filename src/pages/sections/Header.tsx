import { Component, ReactNode } from "react";
import { StyledHeader } from "../../styles/styles";

export default class Header extends Component{
    render():ReactNode{
        return(
            <StyledHeader>Header section</StyledHeader>
        )
    }
}
